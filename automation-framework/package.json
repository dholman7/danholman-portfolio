{
  "name": "automation-framework",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "install:ci": "npm ci || npm install",
    "lint": "echo 'TODO: add ESLint/Prettier' && exit 0",
    "test": "echo 'TODO: add tests' && exit 0",
    "fmt": "echo 'TODO: add format' && exit 0"
  }
}
