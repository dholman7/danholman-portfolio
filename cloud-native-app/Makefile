.PHONY: install lint test fmt

install:
	@echo "No dependencies yet. Add CDK or Lambda setup here."

lint:
	@echo "No linter configured yet."

fmt:
	@echo "No formatter configured yet."

test:
	@echo "No tests yet."
